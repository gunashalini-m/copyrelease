import { mcq } from './helpers.js';

export const theory = [
  mcq('inc-t01', 'Incident table', 'Which table stores ITSM incidents?', ['task', 'incident', 'sc_request', 'problem'], 1, 'Incidents are stored on incident, which extends task.', 'easy'),
  mcq('inc-t02', 'Priority model', 'Incident priority is typically derived from which pair of fields?', ['State and impact', 'Impact and urgency', 'Urgency and assignment group', 'Category and subcategory'], 1, 'The standard priority lookup uses impact and urgency.', 'easy'),
  mcq('inc-t03', 'Caller vs opened_by', 'What does caller_id represent on an incident?', ['The user who last updated the record', 'The person experiencing the issue', 'Always the same as opened_by', 'The assignment group manager'], 1, 'caller_id is the user affected; opened_by is who created the ticket.', 'easy'),
  mcq('inc-t04', 'Active query', 'Which GlideRecord helper limits a query to active=true records?', ['addNullQuery()', 'addEncodedQuery("active")', 'addActiveQuery()', 'setLimit(active)'], 2, 'addActiveQuery() adds the active=true condition.', 'easy'),
  mcq('inc-t05', 'Journal fields', 'work_notes and additional comments (comments) are what type of field?', ['String', 'Journal / journal_input', 'Reference', 'Choice'], 1, 'They are journal fields. Use getJournalEntry(1) for the latest entry in a Business Rule.', 'intermediate'),
  mcq('inc-t06', 'Abort an update', 'How does a Business Rule stop an insert/update from saving?', ['current.deleteRecord()', 'gs.sleep(0)', 'current.setAbortAction(true)', 'gr.setWorkflow(false)'], 2, 'setAbortAction(true) aborts the current database action.', 'easy'),
  mcq('inc-t07', 'Common close state', 'On a typical out-of-box incident, which state value means Closed?', ['1', '2', '6', '7'], 3, 'New=1, In Progress=2, On Hold=3, Resolved=6, Closed=7 (values can vary with customizations).', 'easy'),
  mcq('inc-t08', 'GlideAggregate', 'When should you prefer GlideAggregate over looping GlideRecord to count incidents?', ['Never — GlideRecord is always faster', 'When you only need COUNT/SUM/AVG and not every row', 'When you need to update each record', 'Only in client scripts'], 1, 'Aggregates run in the database. Do not query every row just to count.', 'intermediate'),
  mcq('inc-t09', 'Client mandatory', 'Which client API makes a field mandatory on the form?', ['g_form.setDisplay', 'g_form.setMandatory', 'g_form.setReadOnly', 'gs.addErrorMessage'], 1, 'g_form.setMandatory(field, true) is the client-side API.', 'easy'),
  mcq('inc-t10', 'Events', 'gs.eventQueue is used to:', ['Run a Script Include immediately on the client', 'Queue a named event for notifications, scripts, or flows', 'Commit the current transaction twice', 'Replace Business Rules'], 1, 'Events decouple record updates from emails and other async processing.', 'intermediate'),
  mcq('inc-t11', 'GlideAjax', 'A client-callable Script Include used with GlideAjax should extend:', ['GlideRecord', 'AbstractAjaxProcessor', 'Class.create only', 'g_form'], 1, 'Extend AbstractAjaxProcessor and expose methods; the client passes sysparm_name.', 'intermediate'),
  mcq('inc-t12', 'Parent incidents', 'The parent_incident field is used to:', ['Link an incident to a change', 'Relate child incidents to a parent (often a major incident)', 'Store the caller’s manager', 'Replace problem_id'], 1, 'Child incidents can point at a parent incident for major-incident clustering.', 'easy'),
  mcq('inc-t13', 'Encoded query VIP', 'caller_id.vip=true in an encoded query is an example of:', ['A database join you cannot do in GlideRecord', 'Dot-walking a reference field in a query', 'A client-only API', 'An invalid query'], 1, 'You can dot-walk references in addQuery/addEncodedQuery on the server.', 'intermediate'),
  mcq('inc-t14', 'onSubmit return', 'If an onSubmit client script returns false, what happens?', ['The record is deleted', 'Submit is cancelled', 'The form saves but skips Business Rules', 'The browser reloads'], 1, 'return false blocks form submission.', 'easy'),
  mcq('inc-t15', 'setWorkflow(false)', 'Why call setWorkflow(false) before updating related incidents in a loop?', ['To bypass ACLs permanently for the user', 'To skip Business Rules/engines and reduce recursion or extra processing', 'To force a client script to run', 'To make the update asynchronous'], 1, 'It disables engines for that GlideRecord operation; use carefully.', 'intermediate'),
];
