import { mcq } from './helpers.js';

export const theory = [
  mcq(
    'inc-t01',
    'current.update() in a Business Rule',
    'An interviewer asks why an incident Before Business Rule that calls current.update() is dangerous. What is the most accurate answer?',
    [
      'update() is client-only and will throw on the server',
      'It re-triggers insert/update Business Rules on the same record and can recurse until the platform stops it',
      'It is required so Display Business Rules can see the new values',
      'It skips ACLs, which is always the desired pattern',
    ],
    1,
    'Official guidance: do not use current.update() in a BR. The save that invoked the BR already persists the row; calling update() starts another engine cycle (BRs, workflows, notifications) and can recurse. Use current.setValue() in a Before BR, or an After/Async BR that updates other records. ServiceNow detects recursion and logs it, but you still get extra transactions and race conditions.',
    'hard',
  ),
  mcq(
    'inc-t02',
    'Display BR vs GlideAjax',
    'You need the count of the caller’s open incidents on the incident form. The count must be available as soon as the form loads, without an extra round trip after onLoad. Best pattern?',
    [
      'Client-side GlideRecord in onLoad (getXMLWait)',
      'Display Business Rule that sets g_scratchpad.openCount, then an onLoad Client Script reads g_scratchpad',
      'After Business Rule that gs.addInfoMessage the count',
      'Query Business Rule that injects the count into every list query',
    ],
    1,
    'Display BRs run server-side when the form is loaded and exist to populate g_scratchpad for client scripts. That is the documented way to pass server data that is not on the form. GlideAjax is better when the value must refresh after field changes. Client GlideRecord / getXMLWait is a performance and security anti-pattern. After BRs do not run on form load. Query BRs alter list/database queries, not form scratchpad.',
    'hard',
  ),
  mcq(
    'inc-t03',
    'Query Business Rules',
    'A Query Business Rule on incident adds addQuery("active", true) for users without itil_admin. What is the interview-critical implication?',
    [
      'It only affects the native UI list; GlideRecord in Script Includes still sees inactive incidents',
      'It is appended to queries (including many GlideRecord queries) so scripts running as that user may silently miss inactive rows',
      'It replaces ACLs so you no longer need incident.read',
      'It runs only for encoded queries typed in the filter bar',
    ],
    1,
    'Query BRs modify the query before it executes. They apply more broadly than “the list UI,” including server scripts that query as the user. That is why they are powerful and dangerous: reports, related lists, and integrations can hide records unexpectedly. They are not a substitute for ACLs. Always combine with conditions so they do not fire for system/integration accounts that must see all rows.',
    'hard',
  ),
  mcq(
    'inc-t04',
    'GlideRecordSecure',
    'A Scripted REST API uses GlideRecord("incident") as the authenticated user and returns records the user cannot open in the UI. Why?',
    [
      'REST always runs as admin',
      'Ordinary GlideRecord does not enforce the same end-user ACL semantics; use GlideRecordSecure or explicit canRead/canWrite checks when the script must honor the caller',
      'You forgot setWorkflow(false)',
      'Encoded queries bypass ACLs but addQuery does not',
    ],
    1,
    'Interview staple: GlideRecord in server scripts often runs with elevated/script rights depending on context. GlideRecordSecure applies ACLs as the current user. Table API / UI already go through ACLs; custom Scripted REST that uses raw GlideRecord can leak data. Never assume “the user is logged in, therefore ACLs apply to my script.”',
    'hard',
  ),
  mcq(
    'inc-t05',
    'Counting at scale',
    'A dashboard script does var gr = new GlideRecord("incident"); gr.addQuery("active", true); gr.query(); gs.info(gr.getRowCount()). Why is this a CIS/community performance fail?',
    [
      'getRowCount() is not a real method',
      'GlideRecord.getRowCount() can force the platform to retrieve/count in a way that does not scale; GlideAggregate COUNT (or a report/PA indicator) is the documented approach',
      'You must use chooseWindow() first or getRowCount returns -1',
      'Only client scripts may count records',
    ],
    1,
    'ServiceNow community best practice: do not use GlideRecord to count. GlideAggregate uses the database aggregate path. getRowCount() is associated with loading/counting the result set and is a classic interview trap versus addAggregate("COUNT"). Also setLimit when you only need existence (hasNext).',
    'hard',
  ),
  mcq(
    'inc-t06',
    'N+1 GlideRecord',
    'A scheduled job loops 5,000 incidents and inside the loop queries cmdb_ci by sys_id. What is the expected production failure mode and fix?',
    [
      'It is O(1) because sys_id is indexed, so no change is needed',
      'It can hit the ~5 minute transaction quota; collect CI sys_ids and query once with addQuery("sys_id", "IN", ids) (or cache in a map)',
      'Move the inner query to a Client Script so it does not count against the quota',
      'Replace both queries with g_form.getReference',
    ],
    1,
    'Community “time vs space” guidance: nested GlideRecord is O(N) round-trips. Indexed lookups still pay handshake cost. Batch with IN, or build a hash map. For worker jobs, also consider setWorkflow(false)/autoSysFields(false) only when skipping engines is safe. Client APIs are irrelevant in a scheduled job.',
    'hard',
  ),
  mcq(
    'inc-t07',
    'Priority vs major incident',
    'CIS-ITSM-style: A P1 incident is created. Must it be a Major Incident?',
    [
      'Yes; OOTB P1 always sets major_incident_state=accepted',
      'No. Priority is a data value from impact/urgency; Major Incident is a separate process designation (plugin, roles, communications) and may even apply to a non-P1',
      'Yes if the CI is in the application service table',
      'Only if the caller is VIP',
    ],
    1,
    'Implementation exam trap: P1 ≠ MIM. Activate com.snc.incident.mim, use propose/accept, communication plans, major_incident_manager — not just priority=1. Conversely, an accepted MIM is usually high impact but the process is explicit.',
    'hard',
  ),
  mcq(
    'inc-t08',
    'UI Policy vs Data Policy vs Client Script',
    'Security interview: A client script sets description mandatory for P1, but import sets and Table API still insert P1 incidents with a blank description. What did the implementer miss?',
    [
      'Client scripts run on the Table API if you check "Global"',
      'UI/Client rules do not enforce server/API paths; use a Data Policy (or Before BR / dictionary) so all channels are validated',
      'They needed g_form.setMandatory in an onSubmit as well',
      'Import sets ignore Data Policies too, so only Transform Scripts work',
    ],
    1,
    'Configuration-first interview answer: UI Policy/Client Script = UX. Data Policy can apply to UI and/or web services. Before BR/AbortAction covers all GlideRecord. Transform maps have their own ignore/error flags. Never claim the platform is secure because the form is mandatory.',
    'hard',
  ),
  mcq(
    'inc-t09',
    'Assignment rules and order',
    'Incidents for a CI should go to the CI support group, but some tickets still land on Service Desk. CIS-ITSM implementation thinking: what do you check first besides “is there an assignment rule”?',
    [
      'Only the client onChange of cmdb_ci',
      'Rule order (first match), whether cmdb_ci is populated at insert, CI support_group data quality, and that a later BR/flow is not overwriting assignment_group',
      'Delete all Data Policies on assignment_group',
      'Assignment rules cannot use CI fields',
    ],
    1,
    'Assignment rules evaluate in order; empty CI at insert (caller-created records) skips CI conditions. Dirty CMDB (empty support_group) fails the rule. After rules, lookup rules, flows, and BRs can overwrite. Interviewers want the full chain, not the module name.',
    'hard',
  ),
  mcq(
    'inc-t10',
    'async vs after BR',
    'You must notify a mid-server integration after an incident is committed, but the HTTP call can take 8 seconds. Which BR "When" value is appropriate and why?',
    [
      'before — so the REST call can setAbortAction if the remote side fails',
      'async — the user transaction is not blocked; the job runs after commit on a worker (eventual consistency)',
      'display — Display BRs are the only ones allowed to call RESTMessageV2',
      'after — after BRs are guaranteed not to count against the 5-minute quota',
    ],
    1,
    'Before + outbound REST delays/fails the user save and can hold locks. After still runs in the user session before the response. Async (or eventQueue + Script Action / Flow) is the documented pattern for slow I/O. Display is form-load only. After is still synchronous to the transaction from the user’s perspective for that request’s remaining work.',
    'hard',
  ),
  mcq(
    'inc-t11',
    'Journal fields',
    'In a Before BR, current.work_notes = "x" vs current.work_notes.setJournalEntry("x"). A developer reads getJournalEntry(1) in the same Before BR and gets unexpected results. Why is this subtle?',
    [
      'Journal fields cannot be set in Before BRs at all',
      'Journal writes are special; the in-memory current object / database journal lines may not round-trip like a normal string field in the same execution, so copying “latest journal” in the same BR is a common bug',
      'getJournalEntry only works in Client Scripts',
      'work_notes is encrypted and getJournalEntry always returns empty for itil',
    ],
    1,
    'Community/interview: additional comments and work notes are journal_input. Setting them queues an entry; reading getJournalEntry(1) in the same before script often does not see the line you just assigned the way a normal field would. Prefer After, or copy previous + new value carefully. Do not treat journals as ordinary strings.',
    'hard',
  ),
  mcq(
    'inc-t12',
    'setWorkflow(false) and autoSysFields(false)',
    'Bulk-closing 2,000 child incidents from a parent. Which statement is true?',
    [
      'setWorkflow(false) skips BRs/engines/notifications for that GlideRecord operation; autoSysFields(false) additionally skips sys_updated_on/by style auto fields — both are optimizations with audit/process trade-offs',
      'setWorkflow(false) permanently disables all BRs on the incident table until reboot',
      'autoSysFields(false) is required for ACLs to apply',
      'These APIs only work on import set tables',
    ],
    0,
    'Interview bulk-update pattern: query children, setWorkflow(false) to avoid recursive parent/child BRs and email storms; autoSysFields(false) if you must not churn update stamps (data fix). You lose notifications, SLA engine reactions, and audit friendliness. Never use this on interactive user saves as a default.',
    'hard',
  ),
  mcq(
    'inc-t13',
    'GlideAjax sysparm_name',
    'A GlideAjax call returns empty even though the Script Include method exists. Which is the most common platform-correct cause?',
    [
      'The Script Include is not client-callable, or sysparm_name does not match the method, or the method is not on AbstractAjaxProcessor.prototype',
      'GlideAjax cannot return strings, only XML documents you cannot parse',
      'You must name the parameter sysparm_id or it is ignored',
      'GlideAjax only works for scoped apps in global',
    ],
    0,
    'Checklist: Client callable = true, name matches include, addParam("sysparm_name", "method"), method defined on the prototype extending AbstractAjaxProcessor, ACL/sandbox, and scope. Returning from the method becomes the answer for getXMLAnswer. Empty answer is usually configuration, not “GlideAjax is broken.”',
    'hard',
  ),
  mcq(
    'inc-t14',
    'Caller vs opened_by vs watch_list',
    'A VIP’s EA opens an incident for the VIP. Which data model is correct OOTB intent?',
    [
      'opened_by and caller_id must always be the same user or the form aborts',
      'opened_by is who created the record; caller_id is the affected user; watch_list is extra notification recipients — assignment should not assume they are identical',
      'watch_list replaces caller_id when VIP is true',
      'caller_id is always the assignment group manager',
    ],
    1,
    'Process + reporting: SLA/VIP flags often key off caller_id (affected user), not the fulfiller who clicked New. Notifications: caller, assigned_to, watch_list, assignment group. Interviewers use this to see if you designed for service-desk-on-behalf-of.',
    'hard',
  ),
  mcq(
    'inc-t15',
    'Conditions field on a BR',
    'Why do ServiceNow docs/community insist on filling the Condition field instead of putting if (current.priority == 1) only inside the script?',
    [
      'Conditions are ignored; they are documentation',
      'The condition is evaluated first and can skip loading/running the script entirely, reducing engine cost on every insert/update/query of the table',
      'Conditions run on the client; scripts run on the server',
      'Without a condition the BR cannot access current',
    ],
    1,
    'Every qualifying DB operation evaluates BRs on that table. An empty condition means the script is a candidate every time. Filter in the condition builder (and Filter Conditions) so high-volume incident updates do not parse JS. Still keep script-level guards for race-y field changes.',
    'hard',
  ),
];
