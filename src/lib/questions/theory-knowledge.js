import { mcq } from './helpers.js';

export const theory = [
  mcq(
    'kb-t01',
    'kb_knowledge does not extend task',
    'Why does that CIS note matter when writing a “close task when article publishes” integration?',
    [
      'It does extend task; the note is wrong',
      'You cannot assume number/state/assignment_group/SLA fields from task; knowledge uses workflow_state, valid_to, kb_knowledge_base — different APIs and reporting',
      'Knowledge cannot be queried with GlideRecord',
      'You must store articles on incident',
    ],
    1,
    'CIS-ITSM: kb_knowledge is not a task. Copy-paste incident BR patterns fail (no state 7, no assignment_group unless customized).',
    'hard',
  ),
  mcq(
    'kb-t02',
    'One KB per article',
    'Can a single article belong to two knowledge bases?',
    [
      'Yes, m2m is OOTB required',
      'No — an article is associated to a single kb_knowledge_base; categories nest inside a KB. Duplicate/publish separately or use taxonomy features by family, but don’t assume multi-KB on one row',
      'Yes if you set workflow_state twice',
      'Only if both KBs have the same manager',
    ],
    1,
    'CIS: “A single article may not be associated with multiple knowledge bases.” Interviewers use this to catch people who treat KB like a tag.',
    'hard',
  ),
  mcq(
    'kb-t03',
    'User criteria vs published',
    'Article is published but employees in Partner portal cannot see it. Likely cause?',
    [
      'Published always means internet-public',
      'Can Read / Cannot Read user criteria, KB criteria, or portal widget instance options — publish is necessary but not sufficient',
      'valid_to in the future hides it',
      'helpful_count must be > 10',
    ],
    1,
    'Same idea as catalog user criteria. Knowledge_admin seeing it in native UI proves nothing for the portal audience.',
    'hard',
  ),
  mcq(
    'kb-t04',
    'kb_use',
    'What does kb_use actually measure, and what does it not do?',
    [
      'It is the article body versioning table',
      'It tracks views/attaches to tasks (e.g. incidents); it does not close problems or prove the article fixed the root cause',
      'It stores transform map history',
      'It is a duplicate of sys_view',
    ],
    1,
    'CIS tables: kb_use. Good for “attached to incident” KPIs. Bad as a problem-closure trigger.',
    'hard',
  ),
  mcq(
    'kb-t05',
    'canRead in Script Include',
    'A widget lists published articles via GlideRecord without canRead() and shows HR articles to contractors. Fix?',
    [
      'addQuery("workflow_state", "published") is enough',
      'Use GlideRecordSecure or canRead() per row (and still query criteria-aware APIs where available); published is not an ACL',
      'gs.hasRole("knowledge") hides HR automatically',
      'Client scripts cannot leak knowledge',
    ],
    1,
    'Server widgets/scripted REST are the leak path. User criteria are not always identical to ACLs — test as the contractor.',
    'hard',
  ),
  mcq(
    'kb-t06',
    'Retire vs delete',
    'Legal wants a published article gone from search but retained for eDiscovery. Correct action?',
    [
      'deleteRecord() immediately',
      'Retire (workflow_state retired) / expire valid_to; block delete of published in a BR; deletion is for spam/drafts',
      'Set short_description to empty',
      'Move the row to ecc_queue',
    ],
    1,
    'Audit: retired articles remain. Search filters on workflow_state. Delete loses history.',
    'hard',
  ),
  mcq(
    'kb-t07',
    'valid_to and retroactive search',
    'A job retires articles where valid_to < gs.nowDateTime() but uses getDisplayValue() in the query. Symptom?',
    [
      'Perfect TZ handling',
      'Display vs internal mismatch; query with GlideDateTime/internal or encoded query javascript:gs.endOfToday() style — otherwise some locales never expire or expire early',
      'valid_to cannot be queried',
      'You must use SOAP to read dates',
    ],
    1,
    'Same date discipline as change windows. Community is full of “expiry job doesn’t run in EU.”',
    'hard',
  ),
  mcq(
    'kb-t08',
    'Versioning',
    'Authors edit a published article and users briefly see half-written steps. What OOTB-ish control are they missing?',
    [
      'There is no versioning in ServiceNow knowledge',
      'Checkout/version or draft revision workflow so published stays immutable until a new version is approved — don’t update the live row in place without a process',
      'setWorkflow(false) on every update',
      'Store the body in work_notes of an incident',
    ],
    1,
    'Knowledge versioning (family-dependent UI) exists because in-place edit of published is a production incident. Interview: describe checkout → review → publish.',
    'hard',
  ),
  mcq(
    'kb-t09',
    'Global search vs encoded query',
    'addEncodedQuery("short_descriptionLIKEVPN") misses articles whose VPN steps are only in text HTML. Why?',
    [
      'LIKE never works on knowledge',
      'You searched the wrong field; body is text/html. Full-text/Zing indexing and ts_query APIs differ from field LIKE. Also HTML entities break naive LIKE',
      'You must use GlideAggregate',
      'Knowledge cannot be queried by text by design',
    ],
    1,
    'Search architecture vs GlideRecord. For KCS, use the search API the portal uses, not a homemade LIKE on short_description.',
    'hard',
  ),
  mcq(
    'kb-t10',
    'kb_category nesting',
    'Categories have parent-child. An article in a child category does not show in a widget that queries kb_category=parent. Why?',
    [
      'Parent queries always include children OOTB',
      'The reference is usually a single category sys_id; you must query the subtree (parent OR child IDs), not assume hierarchical expansion',
      'Categories cannot nest',
      'Widgets cannot query kb_knowledge',
    ],
    1,
    'CIS: categories nest. Code often doesn’t. Build the descendant list or use the platform knowledge category APIs.',
    'hard',
  ),
  mcq(
    'kb-t11',
    'Display BR on knowledge',
    'A Display BR on kb_knowledge runs a GlideAggregate COUNT of all articles for a badge. Authors report 8-second form loads. Root issue?',
    [
      'Display BRs are asynchronous and cannot affect load time',
      'Display BRs block form render; heavy queries here serialize every article open. Cache, client GlideAjax lazy-load, or drop the badge',
      'Use getRowCount on kb_knowledge in the client instead',
      'Move the COUNT into a Query BR',
    ],
    1,
    'Developer docs: Display BRs must finish before the user gets the form. They are the wrong place for fleet-wide aggregates.',
    'hard',
  ),
  mcq(
    'kb-t12',
    'Incident.knowledge field',
    'Some instances use knowledge on incident, others m2m / kb_use. Integration copies sys_id into a custom u_kb. Risk?',
    [
      'No risk if the sys_id looks valid',
      'You bypass user criteria, kb_use analytics, and OOTB “used knowledge” related lists — reporting and licensing metrics go wrong',
      'Custom fields automatically feed kb_use',
      'u_kb is an OOTB mandatory field',
    ],
    1,
    'Integrate with the documented related list/API. Custom references are invisible to ITSM dashboards.',
    'hard',
  ),
  mcq(
    'kb-t13',
    'knowledge_manager vs knowledge',
    'Who should own user criteria on a confidential KB?',
    [
      'Any itil fulfiller',
      'knowledge_admin / knowledge_manager (and user_criteria_admin as designed) — not every article author',
      'ecmdb_admin',
      'mid_server',
    ],
    1,
    'CIS role list. Over-granting knowledge_admin is how payroll articles leak.',
    'hard',
  ),
  mcq(
    'kb-t14',
    'Workflow_state vs valid_to both set',
    'workflow_state=published AND valid_to in the past. Search still shows it. What did you forget?',
    [
      'The platform always hides expired published articles with no configuration',
      'Expiry is not magic: you need the job/BR/filter that treats valid_to, or search sources that exclude expired — otherwise state stays published',
      'valid_to is display-only',
      'Zing ignores all dates by design forever',
    ],
    1,
    'Two fields, two engines. Implement the retirement job; don’t assume.',
    'hard',
  ),
  mcq(
    'kb-t15',
    'Scripted REST returning HTML text',
    'You JSON.stringify article.text to a mobile app. XSS/HTML risk?',
    [
      'JSON.stringify strips all HTML',
      'text is HTML; clients must sanitize. Also ACLs: leaking unpublished drafts via a Script Include that ignores canRead is a vulnerability',
      'Knowledge text is always plain text OOTB',
      'REST cannot return HTML fields',
    ],
    1,
    'Security interview. Sanitize, canRead, don’t expose drafts, watch attachments separately (GlideSysAttachment).',
    'hard',
  ),
];
